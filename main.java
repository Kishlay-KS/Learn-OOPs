public class Main { // Class declaration with name Main
    public static void main(String[] args) { // Main method - entry point of the program : args are command line arguments
        System.out.println("Hello, World!");
    }
}

// Static methods - can be accessed without creating an instance of class, through the class itself (Main.main(args);)